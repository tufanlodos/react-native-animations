export type ThemeColors = {
  background: string;
  text: string;
  subText: string;
  border: string;
};

export type InfoData = {
  title: string;
  description: string;
  websiteURL: string;
  repositoryURL: string;
  email: string;
};
